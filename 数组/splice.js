/*
* splice实现删除
* splice(0) 清空数组，返回原有数组所有元素，新数组为空
* splice() ，n也不写把数组一项元素都不删除，返回一个新的空数组，新数组保持原有数组
* splice(n, m): 从索引n开始删除m个(m不写是删除到数组的末尾)
* 返回值: 被删除的内容（以一个新数组保存）
* 原有数组改变
*/

/* 修改
*  let arr = [12, 45, 44, 34]
*  arr.splice(1,1,222,44,[33,44,6])
*  返回[222]
*  新数组为[12, 222, 44, Array(3), 44, 34]
*/

/* 新增
* splice(n, 0, a, b, c): 在索引n项后面新增a, b, c
*
*
*／

let arr = [12, 23, 34]
arr.splice(1,2)